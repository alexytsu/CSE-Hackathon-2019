to build the docker image, run:
sudo docker build -t restapi .
in the directory containing Dockerfile

to run the docker image as a container, run:
sudo docker run -d restapi 

to see all running docker containers, run:
sudo docker ps

to stop a docker container, run:
sudo docker stop [container id]


